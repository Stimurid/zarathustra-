# 3C completion report

## A. BASELINE

- Repo: `C:/projects/zarathustra-push` (`https://github.com/Stimurid/zarathustra-.git`)
- Start branch: `socrates/gs26-real-socrates-and-shiva` @ `cd99bb4` (3B checkpoint; implementation `c2d5833`)
- Work branch: `socrates/3c-aporia-apparatus-learning`
- End SHA: filled after commit

## B. INVENTORY

Existing 3C module was SUBSTRATE_ONLY. Projection lineage, PRESERVE_APORIA, OrganGap, 3B private plane, WM write-deny already live. No AporiaKeeper / OntologyGapEvent class. No second store.

## C. IMPLEMENTATION

Seam: after `run_private_work`, before B2Q-R. New runtime objects: GapKind diagnostic, ApparatusMismatchCandidate, CandidateApparatusChange, ApparatusReplayResult, ApparatusLineageRecord — extending `aporia_and_world_map.py`.

## D. APORIA DIAGNOSTIC

Typed split: ordinary / evidence / operation / projection / ontology / space / genuine aporia / mismatch candidate. User novelty demand ≠ authority.

## E. SAME-MATERIAL REPLAY

`material_ref` shared; compare distinguished vs residue; mixed gain → KEEP_AS_ALTERNATIVE; fact-upgrade or aporia-kill → REJECT.

## F. WORLD-MAP LEARNING

Proposal only. Admit requires review or authorized_transition_ref. History retains predecessor.

## G. LIVE PROOF

SocratesRuntime.run traces in `live_traces/`. CASE1 negative (no false mismatch). CASE5/8 positive candidate+replay. No unauthorized durable write.

## H. TESTS

New CASE 1–12 + substrate. Full backend **1257 passed / 4 skipped / 0 failed**.

## I. GIT

Branch `socrates/3c-aporia-apparatus-learning` (pushed after commit).

## J. REMAINING DEFECTS

D-S26-QSEL-003 still OPEN. Diagnostic is DETERMINISTIC causal (not a new production LLM LIVE suite on the VM). World map is in-process registry, not SQLite.

## K. VERDICT

**3C_PASS_ACTIVE_IN_RUNTIME**
