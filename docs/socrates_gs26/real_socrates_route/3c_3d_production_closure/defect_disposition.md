# 3C+3D production closure — defect disposition

| Defect ID | Verdict | Evidence |
|---|---|---|
| **D-S26-QSEL-003** | **OPEN** (unchanged, nonblocking) | Not the scope of this pass. |
| **D-S26-3D-LIVE-TELOS-001** | **CLOSED** | LIVE A: same context (`ctx_47eb...cc32e8ed`), telos rephrased ("clarify the distinction requested" → "apply the previously established distinction"), dyad `causal_effect = reuse_prior_distinction`, `used_prior_record_ids = ['drec_d35e1e59010f']`, terminal `DISTINGUISH`, surprise `EXPECTED` (not `SCENE_SHIFT`). LIVE B: same context (`ctx_c290...ced6c8f`), explicit user rejection, `user_hypothesis_revised = True`, terminal `PRESERVE_APORIA`. Mechanical CASE A + CASE C green. Repair R1+R2+R3 (`hybrid_dyad.scene_scope_key` + `runtime.prior_scene_key` + legacy scope_id migration on hydration). |
| **D-S26-3C-LIVE-REPEAT-001** | **CLOSED — carrier proven; LIVE mismatch nonclaim** | Mechanical CASE E: injected mismatch across two separate `SocratesRuntime` instances sharing one `SQLiteContextStore` context — turn 1 `PROJECTION_GAP`, turn 2 `APPARATUS_MISMATCH_CANDIDATE`, `durable_write_attempted = False`. Mechanical CASE F: fresh context yields `PROJECTION_GAP`, not `APPARATUS_MISMATCH_CANDIDATE` — no leakage. Mechanical CASE K: `recognition_state.apparatus_repeat` populated after mismatch run — proves carrier rides the existing context snapshot, no new DB. LIVE D: three same-context requests with mismatch-provoking prompts on this LIVE provider chain classified as `EVIDENCE_GAP` (organ_gap) rather than triggering the projection-mismatch signal; `apparatus_repeat` in the persisted `recognition_state` therefore remained empty this run. Handoff §19 criterion 3 admits the "honest documented architectural nonclaim with owner-grade evidence" formulation for the LIVE half — recorded in `production_live_acceptance.md` §LIVE D. |
| **D-S26-3C-LIVE-ORGAN-PRIORITY-001** | **CLOSED** | LIVE B2 and LIVE H2 both produced `terminal = PRESERVE_APORIA` on the deployed SHA. Before the repair these would have classified as `EVIDENCE_GAP`; after the repair both classify as `GENUINE_APORIA` with grounds `['typed_source_or_organ_gap', 'contributing:typed_source_or_organ_gap', 'preserve_aporia_terminal_promoted_over_evidence_gap']` — the specific gap type retained as a contributing ground. Mechanical CASE J covers both the positive path (`PRESERVE_APORIA + SOURCE_GAP` → `GENUINE_APORIA`) and the negative guard (non-aporia terminal + `SOURCE_GAP` still classifies as `EVIDENCE_GAP`). Repair R5 (`aporia_and_world_map.run_apparatus_diagnostic` post-chain promotion). |
