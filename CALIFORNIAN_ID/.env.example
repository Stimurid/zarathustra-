# Copy to .env and fill values you want to use.
# The default `mock` provider needs no keys.

# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=

# Override the runtime provider without touching config/models.yaml:
# CALIFORNIAN_ID_PROVIDER=anthropic
