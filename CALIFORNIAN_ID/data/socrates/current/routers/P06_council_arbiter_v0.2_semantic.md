# P06 — semantic router v0.2 — candidate

**Generation:** G-S25R.7  
**Status:** candidate / router-only  
**Pipeline phase(s):** S7  
**Purpose:** minimum-sufficient council and arbitration.

## 1. Authority and anti-compression boundary
This file is an orchestration router. It carries **no independent Socratic doctrine**. Semantic meaning comes from the exact full semantic bodies mounted below; legal/representable execution comes from the hard contracts and invariants below. If a required body cannot be loaded by exact identity and version, fail explicitly. Do not substitute its runtime summary, an embedding hit, a historical predecessor, model prior, or historical G-S25 tiny prompt.

Historical G-S25 `P06_council_arbiter.md` remains immutable pre-repair control evidence and is forbidden as a semantic fallback.

## 2. Mandatory semantic mounts
Mount these **before interpreting this router's operation**:
- CORE — Drive `123QTjcjJjQT1m-tsUsGwFaGpDaKgCKOG` — `SOCRATES_CORE_SEMANTIC_BODY_v0.2_candidate.md` — semantic_version `0.2` — FULL BODY REQUIRED
- B09 — Drive `1-ckkQv4PFl2642YOTBe4gf-eGEL4xdg-` — `B09_POSITIONAL_ECOLOGY_COUNCIL_ARBITRATION_v0.2_candidate.md` — semantic_version `0.2` — FULL BODY REQUIRED

Verify `body_id + exact Drive ID + semantic_version`. Record `loaded_full_body=true` and source-binding verification in MountTrace.

## 3. Conditional semantic mounts
- B02: mount only when one of `STATUS_DISPUTE, TEMPORAL_STALE, PROVENANCE_DISPUTE, BINDING_HISTORY_MATERIAL` is present in typed state; exact Drive `1QNAptEERaQ9hQtZOQf1MYL-SKmKEM8N4`
- B07: mount only when one of `REFLECTIVE_EXIT_REQUIRED, ROLE_CAPTURE, FRAME_GENERATED_FAILURE, SELF_REVIEW_RECURSION` is present in typed state; exact Drive `1ud_Und-YnqiXUTdXr8hbgfLsWxvaiqLF`
- B08: mount only when one of `MULTI_ONTOLOGY, OBJECT_GENERATOR_LIMIT, FIELD_MODE_REQUESTED, CONSTRUCTIVE_BRANCH` is present in typed state; exact Drive `1V-HHNfVbi9K5ae21PoJ6TOU1CTmYa-KO`

Conditional bodies are not retrieval choices. Mount only from typed triggers, and log the trigger IDs. When triggered they become non-droppable for this step.

## 4. Entry semantics
Enter only when current pipeline state is one of: `S7`. Preserve current typed state, provenance, authority/ownership, open loops, minority/aporia refs and prior accepted bindings. This router may select/mount context and request the phase operation; it may not manufacture evidence, authority, ownership, status or a new worldview.

## 5. Hard contract surface
**Inputs:** `council_recipe`, `organ_contribution`, `persona_adapter`.  
**Outputs:** `arbitration_record`.  
**Critical invariants:** `INV-011`, `INV-012`, `INV-013`, `INV-014`.  
`prompt_only_critical_invariant=false` remains mandatory.

Hard contracts constrain legality; mounted semantic bodies govern interpretation. If the two materially disagree, emit `SEMANTIC_CONTRACT_DRIFT` and stop/return rather than silently resolving the contradiction.

## 6. Context budget rule
Drop optional layers only in this order: style/rhetoric extras → optional retrieval background → optional persona/donor lenses → optional examples/countercases. Never drop or summarize CORE, required phase bodies, or a triggered conditional body. If mandatory context still cannot fit, emit `SEMANTIC_CONTEXT_BUDGET_EXCEEDED`.

## 7. Operation and output discipline
Execute the phase only after mount verification. Keep internal trace to typed state/transition/evidence/authority/mount records; do not request or expose private chain-of-thought. User-visible output permission for this router is `false`. When visible output is allowed, render only from authorized current state and preserve uncertainty/conflict required by mounted bodies and contracts.

## 8. Exit / failure
Successful exit requires: required mounts verified; triggered conditional mounts resolved; declared output contract satisfied; invariant check passed; no unauthorized state write; MountTrace emitted. Valid explicit failures include `SEMANTIC_MOUNT_MISSING`, `SEMANTIC_VERSION_MISMATCH`, `SEMANTIC_SUMMARY_SUBSTITUTION`, `SEMANTIC_CONTEXT_BUDGET_EXCEEDED`, `SEMANTIC_CONTRACT_DRIFT`, `SOURCE_BINDING_MISSING`.

Failure policy: `FAIL_EXPLICIT_OR_RETURN_OPERATION`. Never silently fall back to the historical prompt pack.
